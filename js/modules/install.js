import { command } from "../core/worker.js";import { deviceOptions } from "../core/ui.js";
export function render(root){root.innerHTML=`<section class="card"><h2>Install / Setup Status</h2><p>Checks Windows Agent v2 install, version/config, startup hints, and cleanup report.</p><select id="idev" data-device-select>${deviceOptions(d=>d.type==="windows")}</select><div class="button-grid"><button id="getHealth">Get Agent Health</button><button id="getConfig">Get Config</button><button id="getCleanup">Get Cleanup Report</button><button id="runCleanup" class="danger">Run Old Cleanup</button><button id="getErrors">Get Recent Errors</button></div><pre id="installOut">Results will appear in dirty logs / command result stream.</pre></section>`;const dev=()=>document.getElementById("idev").value;document.getElementById("getHealth").onclick=()=>command("GET_AGENT_HEALTH",{},dev());document.getElementById("getConfig").onclick=()=>command("GET_AGENT_CONFIG",{},dev());document.getElementById("getCleanup").onclick=()=>command("GET_CLEANUP_REPORT",{},dev());document.getElementById("runCleanup").onclick=()=>confirm("Run old version cleanup?")&&command("RUN_OLD_VERSION_CLEANUP",{},dev());document.getElementById("getErrors").onclick=()=>command("GET_RECENT_ERRORS",{lines:100},dev());}