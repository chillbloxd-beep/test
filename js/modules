import { state } from "../core/state.js";
export function render(root){root.innerHTML=`<section class="card"><h2>Settings</h2><p>Edit <span class="kbd">config.js</span> in the repo to change backend URL, room, key, and defaults.</p><pre>${JSON.stringify(state.cfg,null,2)}</pre></section>`;}
